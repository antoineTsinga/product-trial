export enum ControlType {
  AUTOCOMPLETE = "autocomplete",
  CHECKBOX = "checkbox",
  DATE = "date",
  INPUT = "input",
  LIST = "list",
  MULTISELECT = "multiselect",
  NUMBER = "number",
  SELECT = "select",
  TABLE = "table",
}
